# Equivalente práctico a legacy-peer-deps (LangChain / checkpoints).
auto-install-peers=true
strict-peer-dependencies=false
# node_modules plano: mejor con Nest, Prisma y Docker (antes npm hoisting).
node-linker=hoisted
