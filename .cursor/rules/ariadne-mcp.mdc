---
description: Contrato MCP AriadneSpecs (HTTP Streamable) frente al monorepo Ariadne y al cliente Nest The Forge
globs: apps/api/src/modules/theforge/**, docs/notebooklm/integracion-theforge/**, "**/.env.example"
alwaysApply: false
---

# Ariadne MCP ↔ The Forge

- **Normativo (repo Ariadne):** `docs/MCP_HTTPS.md`, `docs/mcp_server_specs.md`, `docs/MCP_AYUDA.md` (posible copia en `docs/notebooklm/`).
- **Espejo en The Forge:** `docs/notebooklm/integracion-theforge/Llamadas-HTTPS-MCP-AriadneSpecs.md`, `SPEC-MCP-001-THEFORGE.md`.
- **Cliente HTTP:** `THEFORGE_MCP_URL` → `TheForgeService.postTheForgeMcp` — JSON-RPC `tools/call`, headers `MCP-Protocol-Version: 2025-03-26`, `Accept: application/json, text/event-stream`, auth `MCP_AUTH_TOKEN` o `MCP_X_M2M_TOKEN` (no confundir con JWT Nest del proceso MCP: `ARIADNE_API_BEARER` / `ARIADNE_API_JWT` en el **servidor** MCP).
- **IDs:** `list_known_projects.id` = proyecto workspace; `roots[].id` = repo. Resolución en `ariadne-mcp-scope.util.ts` para qué UUID pasar a `ask_codebase` / grafo / `semantic_search`.
- **Contrato de argumentos:** `theforge-mcp-client-contract.ts`; humo: `npm run test:mcp-alignment -w @theforge/api` (raíz monorepo) con `THEFORGE_MCP_URL`.
- **Consulta desde la API (legacy / doc. partida):** `docs/notebooklm/integracion-theforge/ARIADNE-CONSULTA-DESDE-THEFORGE.md` — timeout MCP, no usar `limit` ridículo en `semantic_search` (suelo ReAct), `ask_codebase` secuencial vs paralelo (`LEGACY_CODEBASE_DOC_PARALLEL_ASK`).
