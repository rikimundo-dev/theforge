# Perfil local full-docker (puertos en host, sin Traefik).
# Uso: docker compose -f docker-compose.yml -f docker-compose.local.yml up --build
#
# App: http://localhost:8080  (nginx proxy /api → theforge-api)
# API directa (debug): http://localhost:3000
# Adminer: http://localhost:8081

services:
  theforge-db:
    ports:
      - "5432:5432"

  theforge-api:
    ports:
      - "3000:3000"

  theforge-web:
    ports:
      - "8080:80"

  theforge-adminer:
    ports:
      - "8081:8080"
