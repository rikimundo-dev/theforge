# store

Estado global con Zustand.

| Archivo              | Uso                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **workshopStore.ts** | **`selectWorkshopAgentsBusy`** / `isWorkshopAgentsBusy`: navegación al panel bloqueada si agentes trabajan. **`fetchProject`** limpia stream al cambiar de proyecto y solo aplica sesión/hilo si `projectId` del store coincide; **`sendMessage`** etiqueta `requestProjectId` y descarta chunks/done de otro proyecto. **`reset()`** al salir del Workshop (App). **`legacyGenerateCodebaseDoc(projectId, { responseMode })`** — POST `…/legacy/generate-codebase-doc` con body JSON (`responseMode` alineado con Ariadne `ask_codebase`). Sin segundo argumento, body `{}` (comportamiento API por env). **`patchWorkshopStage(stageId, body)`** — PATCH `…/projects/:id/stages/:stageId` (BRD, MDD, etc.). **`legacyGenerateAsIsManual`**, **`legacySuggestBrdFromCodebaseDoc`** — POST legacy As-Is / sugerencia BRD desde `codebaseDoc`. **`suggestBrdFromDbga(projectId, { stageId? })`** — POST `…/projects/:id/suggest-brd-from-dbga` (greenfield, `dbgaContent`; `loadingReason` `brd-from-dbga`). To-Be/As-Is eliminados (Jul 2026) — solo BRD. Todas las llamadas al backend usan **`apiFetch`** (`utils/apiClient`) con JWT. Exporta **`sessionMessageBody`** para `POST /sessions/:id/messages` con `stageId` opcional e **`images`** opcionales en mensajes user. Store del Workshop: project (incluye **`stages[]`**, **`complexityPending`** HITL), **`workshopStages`** (copia sincronizada de `stages[]` para el selector multi-etapa), **`activeStageId`** (vista en vivo MDD), session, mddContent, loading, synced, error, **agentProgress**, **evaluatorCritique**. **`LiveMetricsResult`**: `roles` / `rolesHours` como `Record<string, number>`, **`readinessHints`** opcional (pistas IA/cascadas desde el estimador). **`conformance.blueprintDataModel`**: alineación Blueprint vs **§3 modelo de datos** del MDD; si `ok === false`, **`generateApiContracts`** no llama al API (y el backend también devuelve 400). **`confirmComplexityProposal` / `dismissComplexityProposal`** para el banner de complejidad. Tras stream DBGA (`/ai-analysis/stream`), si el `done` incluye `complexityProposal`, se actualiza el proyecto en memoria antes de `fetchProject`. |
