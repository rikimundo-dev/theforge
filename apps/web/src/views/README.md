# views

Vistas de la aplicación web.

| Componente           | Uso                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **LoginView.tsx**    | Login OTP (`/auth/otp/request`, `/auth/otp/verify`); persiste JWT con `utils/apiClient`. Solo el correo permitido en API recibe el código.                                                                                                                                                                                         |
| **WorkshopView.tsx** | Vista principal: 3 columnas — ChatContainer (**Re-Valorar** → `reassess-complexity` + primer mensaje de entrevista); tabs de documentos; semáforo + costos + auditoría. **Banner `ComplexityPendingBanner`:** si hay propuesta HITL (`complexityPending`), confirmar/descartar o por chat. Header: selector **vista en vivo (etapa)** desde **`workshopStore.workshopStages`** + **Nueva etapa** (modal: nombre + **copiar MDD desde** cualquier etapa o vacío → `createWorkshopStage` con `copyMddFromStageId`). Panel **Conformance**: línea **Blueprint vs MDD §3**; generación de **Contratos API** deshabilitada en UI si hay gaps §3. **Ayuda** → `WorkshopHelpModal` (`content/workshop-manual.md`); **Descargar todo (ZIP)**. **Paso 0:** Benchmark stream → `dbgaContent` + `complexityPending`; Deep Research → `phase0SummaryContent`. **`complexity === LOW`:** oculta MDD, Blueprint, API; **Generar entregables** → `generateDeliverablesCascade`. **`complexity === MEDIUM`:** `isTabVisibleForComplexity` (NEW/LEGACY). **Legacy:** tab Modificación. Zustand. |

Se entra desde la lista de proyectos en App; se pasa `projectId`, `projectName` y `onBack`.
